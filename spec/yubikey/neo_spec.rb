require 'spec_helper'

describe Yubikey::Neo do
  it 'has a version number' do
    expect(Yubikey::Neo::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
